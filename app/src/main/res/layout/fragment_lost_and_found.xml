<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#D6F1C0">

    <!-- Filter and Search -->
    <LinearLayout
        android:id="@+id/filter_and_search_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:padding="2dp"
        android:background="#ADF471"
        android:orientation="horizontal"
        tools:ignore="MissingConstraints">

        <!-- Filter -->
        <Spinner
            android:id="@+id/filter_spinner"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="3.5dp"
            android:layout_gravity="center_vertical"
            android:entries="@array/filter_options" />

        <!-- Search -->
        <androidx.appcompat.widget.SearchView
            android:id="@+id/search_view"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginRight="5dp"
            android:background="@null"
            android:iconifiedByDefault="false"
            android:maxWidth="600dp"
            android:layout_gravity="center_vertical"
            android:queryHint="@string/search_hint"
            app:iconifiedByDefault="false"
            app:queryHint="@string/search_hint"/>
    </LinearLayout>

    <!-- RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/lost_and_found_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/row_lostandfound"
        android:layout_below="@id/filter_and_search_layout"/>
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_create_item"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="90dp"
        android:layout_marginRight="10dp"
        android:background="@color/primary_blue"
        android:contentDescription="create_item"
        android:src="@drawable/ic_add"
        tools:ignore="MissingConstraints" />

</RelativeLayout>
